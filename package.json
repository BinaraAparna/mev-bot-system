{
  "name": "mev-bot-contracts",
  "version": "1.0.0",
  "description": "MEV Bot Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network polygon"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "hardhat": "^2.19.2",
    "ethers": "^5.7.2",
    "chai": "^4.3.10"
  }
}